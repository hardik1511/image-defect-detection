modules = ["python-3.10"]

[deployment]
run = ["sh", "-c", "cd backend && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 5000"]

[[ports]]
localPort = 5000
externalPort = 80
